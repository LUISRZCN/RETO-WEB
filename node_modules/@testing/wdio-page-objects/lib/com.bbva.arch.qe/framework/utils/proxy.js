"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AbstractProxy = void 0;
/**
 * @hidden
 */
class AbstractProxy {
    getDescription(target, property) {
        const desc = Object.getOwnPropertyDescriptor(target, property);
        const propotype = Object.getPrototypeOf(target);
        return (desc) || (propotype && this.getDescription(propotype, property));
    }
    get(target, method) {
        const propDesc = this.getDescription(target, method);
        const targetMethod = propDesc && propDesc.get ? propDesc.get : target[method];
        if (propDesc && propDesc.get) {
            return this.rewrite(targetMethod).apply(target);
        }
        else if (propDesc && typeof propDesc.value === 'function') {
            return this.rewrite(targetMethod);
        }
        else {
            return targetMethod;
        }
    }
}
exports.AbstractProxy = AbstractProxy;
//# sourceMappingURL=proxy.js.map